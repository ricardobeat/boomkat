duktape
*.dSYM
out/
# Upstream QuickJS checkout: a benchmark engine fetched by `just fetch-quickjs`.
# The four files the BUILD needs are vendored in vendor/dtoa/ instead.
quickjs/
.antigravitycli
.worktrees/
worktrees/
ctest2
dist/

# TypeScript conformance corpus (sparse clone, fetched by scripts/fetch_ts_conformance.py)
test/typescript/conformance-src/
test/typescript/ts_conformance_cache/

# Real-world library corpus (fetched by scripts/verify_libraries.py), not vendored
test/libcorpus/
.claude

# TypeScript runtime-library sources (fetched by scripts/verify_ts_libraries.py)
test/tscorpus/
test262_results

# Internal commit-attribution analysis (private session/prompt data)
.attrib/
qemu_*.core
__pycache__/
